<h1><%= pluralize(@users.count, "user") %></h1>
<ul class="mt-5">
  <% @users.each do |user| %>
    <li>
      <%= link_to user_path(user) do%>
        <h3>
          - <%= user.name %>: <%= user.email %>
        </h3>
      </li>
    <% end %>
  <% end %>
</ul>
