<h1><%= pluralize(@users.count, "user") %></h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user_path(user) do%>
        - <%= user.name %>: <%= user.email %>
      </li>
    <% end %>
  <% end %>
</ul>
